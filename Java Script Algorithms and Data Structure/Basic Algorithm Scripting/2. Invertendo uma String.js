function reverseString(str) {
  return str
  // Separa caracter por caracter
    // Pode ser usado nos desafios de conclusao
    .split("")
  // Inverte o array criado
    .reverse()
  // Junta num so
    .join("");
}

console.log(reverseString("arara"));