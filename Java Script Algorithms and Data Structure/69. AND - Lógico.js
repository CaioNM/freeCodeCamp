// Criando função
  // Aprendendo Operador logico "AND"
    // Operador lógico "AND" indica que duas coisas tem que acontecer e é representado por "&&", dessa forma:
function testLogicalAnd(val) {
  if (val<=50 && val>=25) {
      return "Yes";
  }
  return "No";
}

testLogicalAnd(10);