// Passando argumentos as funções:

// Criando a função
  // Essa função aceita dois valores e soma eles (ou concatena)
function functionWithArgs(parametro, parametro1){
  console.log(parametro + parametro1);
}

// Chamando
functionWithArgs(1, 2);