// Declarando um array
const myArray = [["John", 23], ["cat", 2]];

// É possível adicionar novos itens a um array pelo metodo "push()"
  // Aqui coloco um array dentro do array myArray
myArray.push(["dog", 3])