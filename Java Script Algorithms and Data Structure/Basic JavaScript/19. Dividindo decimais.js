// Divisão com número decimal:
  // Criando uma operação para que o valor de 'quotient' seja igual a 2.2
    // Obs.: Eu alterei esse código pra tentar dividir um float por um int para ver se funcionava, e deu certo!
const quotient = 4.4 / 2; 
