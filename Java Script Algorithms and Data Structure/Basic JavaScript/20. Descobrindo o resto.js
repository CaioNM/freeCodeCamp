// Descobrindo "resto"
  // Para descobrir o resto de uma divisão usa-se '%', é feita uma divisao entre dois números e o resto dela é descoberto, se for uma divisão exata,  resultará em 0
const remainder = 11%3;