// Definindo variaveis
let firstLetterOfLastName = "";
const lastName = "Lovelace";

// Descobrindo um caracter pelo seu index.
  // Segue o padrâo de qualquer linguagem, começa em 0 e é so colocar entre colchetes "[]"
    // Atribui a primeira letra de "lastName" a variavel "firstLetterOfLastName"
firstLetterOfLastName = lastName[0];