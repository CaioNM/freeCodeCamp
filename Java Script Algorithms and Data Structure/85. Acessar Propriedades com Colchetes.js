// Objeto criado
const testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

// Outra forma de chamar um atribuito de um objeto, é por colchetes, normalmente usado quando o nome tem espaços no meio
const entreeValue = testObj["an entree"];
const drinkValue = testObj["the drink"];