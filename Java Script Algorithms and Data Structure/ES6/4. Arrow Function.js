//Criando uma variável anônima
  // Uma variavel anonima é criada quando não é necessário nomear uma função e ela não será utilizada no futuro
    // Para cria-la, usa-sé "=>", dessa forma:
const magic = () => {
  return new Date();
};

console.log(magic());